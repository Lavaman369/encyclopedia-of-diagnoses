{
  "name": "ph-diagnostics",
  "version": "1.0.0",
  "description": "Project Hospital game encyclopedia and diagnostic tool",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "npx serve docs -p 3000"
  },
  "devDependencies": {
    "fast-xml-parser": "^4.3.6"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
